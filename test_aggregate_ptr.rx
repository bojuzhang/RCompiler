struct Node {
    x : i32,
    y : i32
}

impl Node {
    fn new() -> Node {
        Node { x: 0, y: 0 }
    }
    
    fn sum(&self) -> i32 {
        self.x + self.y
    }
}

fn main() {
    let node = Node::new();
    let sum = node.sum();
    exit(0);
}