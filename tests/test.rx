struct Node {
    x : i32,
    y : i32,
}

fn foo(node : Node) -> i32 {
    node.x + node.y
}

fn main() {
    let mut node : Node = Node{x : 1, y : 2};
    let ans : i32 = foo(node);
    printlnInt(ans);
    exit(0);
}