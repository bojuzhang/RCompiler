struct Node {
    x : i32,
    y : i32
}

impl Node {
    fn sum(self) -> i32 {
        self.x + self.y
    }
}

fn main() {
    let a : i32 = getInt();
    let b : i32 = getInt();
    let node : Node = Node {x : a, y : b};
    printlnInt(node.sum());
    exit(0);
}