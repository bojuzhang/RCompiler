fn main() {
    let mut a : i32 = 1;
    let mut b : i32 = 1;
    let test : i32 = loop {
        a = a - 1;
        if (a > 0) {
            break 5;
        }
        break 10;
    };
    printlnInt(test);
    exit(0);
}