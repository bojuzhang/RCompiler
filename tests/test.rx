struct Node {
    x : i32,
    y : i32,
    arr : [i32; 2]
}

fn main() {
    let mut node : Node = Node {x : 1, y : 2, arr : [1, 2]};
    printlnInt(node.y);
    printlnInt(node.arr[1]);
    exit(0);
}