struct Node {
    x : i32,
    y : i32
}

impl Node {
    fn get_val(self) -> i32 {
        self.x * 2 + self.y * 2
    }
}

fn main() {
    let mut node : Node = Node {x : 1, y : 2};
    printlnInt(node.get_val());
    exit(0);
}