struct Node {
    x : i32,
    y : i32
}
fn main() {
    let mut a : [Node; 2] = [Node {x : 1, y : 2}; 2];
    let mut b : [Node; 2] = [Node {x : 42, y : 42}; 2];
    a[0] = b[0];
    printlnInt(a[0].x);
    exit(0);
}