fn foo(flag : bool) -> bool {
    flag
}

struct Node {
    x : i32,
    y : i32
}

impl Node {
    fn goo(self) -> i32 {
        self.x
    }
}


fn main() {
    let flag : bool = true;
    let node : Node = Node {x : 10, y : 20};
    foo(flag);
    node.goo();
    exit(0);
}