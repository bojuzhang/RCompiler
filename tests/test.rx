struct Node {
    arr : [i32; 10],
    val : i32
}
fn foo(node : &mut Node) -> i32 {
    node.arr[1]
}
fn goo(node : Node) -> i32 {
    node.arr[1]
}
fn main() {
    let node : Node = Node {arr : [42; 10], val : 1};
    printlnInt(foo(&mut node));
    printlnInt(goo(node));
    exit(0);
}