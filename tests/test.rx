/*
struct Node {
    x : i32,
    y : i32
}

struct Tree {
    node : Node,
    tmp : i32
}

impl Node {
    fn get(&self) -> Node {
        Node {x : self.x, y : self.y}
    }
}

impl Tree {
    fn get_node(&self) -> Node {
        self.node
    }
}
*/

struct Node {
    arr : [i32; 3]
}

impl Node {
    fn modify(&mut self, x : i32) {
        self.arr[x as usize] += 114514;
    }
}

fn main() {
    /*
    let node : Node = Node {x : 42, y : 42};
    let tree : Tree = Tree {node : node, tmp : 114514};
    let t : Node = tree.get_node().get();
    printlnInt(t.x);
    */

    let mut node : Node = Node {arr : [1, 2, 3]};
    node.modify(1);
    printlnInt(node.arr[1]);
    
    exit(0);
}