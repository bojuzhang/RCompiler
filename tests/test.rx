struct Node {
    x : i32,
    y : i32
}

fn main() {
    let mut node : Node = Node {x : 42, y : 42};
    let ptr : &mut Node = &mut node;
    printlnInt(ptr.x);
    ptr.x = 114514;
    printlnInt((*ptr).x);
    exit(0);
}