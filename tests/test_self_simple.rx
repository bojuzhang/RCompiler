struct Point {
    x : i32,
    y : i32
}

impl Point {
    fn test_self(&self) -> i32 {
        self.x
    }
}

fn main() {
    let p : Point = Point { x : 5, y : 7 };
    let result : i32 = p.test_self();
    printlnInt(result);
    exit(0);
}